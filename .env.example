# Copy to .env and fill in. Never commit .env.
ALLOWED_LOGIN=you@example.com
HERDR_UID=1000
HERDR_GID=1000

# Which released version to run. Unset means the newest release.
# Pin it to roll back, e.g. HERDR_PWA_VERSION=0.1.0
# HERDR_PWA_VERSION=
